# Server-side only — never shipped to the browser. Exchanged for a short-lived,
# model-scoped JWT by POST /api/token (vite dev middleware and server/serve.mjs).
REACTOR_API_KEY=rk_...

# Hosted Reactor world model the "Generated brain" view connects to.
VITE_REACTOR_MODEL=reactor/lingbot-world-2
